import BulkLoad from 'tedious/lib/bulk-load';
import Connection, { type ConnectionAuthentication, type ConnectionConfiguration, type ConnectionOptions } from 'tedious/lib/connection';
import Request from 'tedious/lib/request';
import { ConnectionError, RequestError } from 'tedious/lib/errors';
import { TYPES } from 'tedious/lib/data-type';
import { ISOLATION_LEVEL } from 'tedious/lib/transaction';
import { versions as TDS_VERSION } from 'tedious/lib/tds-versions';
declare const library: {
    name: string;
};
export declare function connect(config: ConnectionConfiguration, connectListener?: (err?: Error) => void): Connection;
export { BulkLoad, Connection, Request, library, ConnectionError, RequestError, TYPES, ISOLATION_LEVEL, TDS_VERSION };
export type { ConnectionAuthentication, ConnectionConfiguration, ConnectionOptions };
