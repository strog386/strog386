import { type Metadata } from 'tedious/lib/metadata-parser';
import Parser from 'tedious/lib/token/stream-parser';
import { ColMetadataToken } from 'tedious/lib/token/token';
export interface ColumnMetadata extends Metadata {
    /**
     * The column's name。
     */
    colName: string;
    tableName?: string | string[] | undefined;
}
declare function colMetadataParser(parser: Parser): Promise<ColMetadataToken>;
export default colMetadataParser;
