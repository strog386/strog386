import { EventEmitter } from 'events';
import { type ParserOptions } from 'tedious/lib/token/stream-parser';
import Debug from 'tedious/lib/debug';
import { Readable } from 'stream';
import Message from 'tedious/lib/message';
import { TokenHandler } from 'tedious/lib/token/handler';
export declare class Parser extends EventEmitter {
    debug: Debug;
    options: ParserOptions;
    parser: Readable;
    constructor(message: Message, debug: Debug, handler: TokenHandler, options: ParserOptions);
    on: (((event: 'end', listener: () => void) => this) & ((event: string | symbol, listener: (...args: any[]) => void) => this));
    pause(): Readable;
    resume(): Readable;
}
